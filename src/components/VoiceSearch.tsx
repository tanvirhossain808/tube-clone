"use client"

// import useVoiceApi from "@/hooks/useVoiceApi";
const VoiceSearch: React.FC = () => {
    // const { startVoiceRecognition } = useVoiceApi()

    return (
        // <button onClick={startVoiceRecognition} type="button" className="block h-[30px] w-[30px] rounded-full bg-gray-600 ml-5">🎤</button>
        ""
    )
}

export default VoiceSearch
